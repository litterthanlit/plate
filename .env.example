# Server-only Places proxy. Do not prefix with NEXT_PUBLIC_.
# Enable Places API (New) on this key and restrict it to that API.
GOOGLE_PLACES_API_KEY=

# Optional fallback name if you already created a Maps key:
# GOOGLE_MAPS_API_KEY=
